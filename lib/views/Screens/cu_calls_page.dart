import 'package:flutter/cupertino.dart';

class cu_calls_page extends StatelessWidget {
  const cu_calls_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}